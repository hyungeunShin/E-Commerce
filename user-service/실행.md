## Edit Configurations -> Add VM options -> -Dserver.port=60001

## Terminal

```shell
./gradlew bootRun --args='--server.port=60002'
```

```shell
cd build/libs
java -jar -Dserver.port=60003 user-service-0.0.1-SNAPSHOT.jar
```

