spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
#      name: ecommerce # 파일명 지정
#      name: config-service # 프로젝트명과 동일하면 application.yml
      name: user-service

#  profiles:
#    active: dev