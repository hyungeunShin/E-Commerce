server:
  port: 8761

spring:
  application:
    name: discovery-service

  output:
    ansi:
      enabled: always

eureka:
  server:
    enable-self-preservation: false
  client:
    register-with-eureka: false
    fetch-registry: false