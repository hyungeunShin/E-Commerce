server:
  port: 8888

spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: file:///C:/intellij_workspace/ecommerce-config # .git? ???? ?
          default-label: master